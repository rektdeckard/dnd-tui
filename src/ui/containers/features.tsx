import React from "react";
// import { Box } from "ink";

import BorderBox from "../components/BorderBox";

const FeaturesLayout: React.FC<{}> = () => {
  return <BorderBox flexGrow={1} marginLeft={1} />
};

export default FeaturesLayout;
